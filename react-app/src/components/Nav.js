import React from 'react';


const Nav = (props) => {
    return (
        <nav>
            <button>Cats</button> 
            <button>Dogs</button> 
            <button>Birds</button> 
        </nav>
    )
}

export default Nav;